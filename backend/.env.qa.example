# LifeLens backend — QA environment template.
# Real values are injected by CI (GitHub Environment `qa`) / locally. NEVER commit real values.

APP_ENV=qa
DEBUG=true

CORS_ORIGINS=https://api-qa.lifelens.app

DATABASE_URL=

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

GEMINI_API_KEY=
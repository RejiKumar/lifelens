# LifeLens backend — PROD environment template.
# Real values are injected by CI (GitHub Environment `prod`) / Render. NEVER commit real values.

APP_ENV=prod
DEBUG=false

CORS_ORIGINS=https://api.lifelens.app

DATABASE_URL=

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

GEMINI_API_KEY=